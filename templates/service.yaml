apiVersion: v1
kind: Service
metadata:
  name: {{ include "afraid.fullname" . }}
  labels:
    {{- include "afraid.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "afraid.selectorLabels" . | nindent 4 }}
