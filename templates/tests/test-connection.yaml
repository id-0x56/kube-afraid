apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "afraid.fullname" . }}-test-connection"
  labels:
    {{- include "afraid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "afraid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
